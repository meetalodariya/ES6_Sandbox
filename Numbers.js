let n = 3.87;
let num2 = -48.3;

console.log(Math.trunc(n));
console.log(Math.sign(num2)); // returns -1 if number is negative otherwise 1 and returns 0 for 0

let number = NaN;
console.log(Number.isNaN(number)); // checks whether given number is NaN(Not a Number) or not

number = 10/0;
console.log(Number.isFinite(number)); // checks whether given number is finite or not







